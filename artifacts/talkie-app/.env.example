# --- Frontend ---
VITE_API_URL=http://localhost:4000
VITE_WS_URL=ws://localhost:4000

# --- Backend ---
# Le backend (/backend) a son propre fichier d'environnement : voir
# backend/.env.example (DATABASE_URL, JWT_SECRET, PORT, CORS_ORIGIN,
# WEB_PUSH_PUBLIC_KEY / WEB_PUSH_PRIVATE_KEY depuis la Phase 9...).
# Le frontend n'a pas besoin de sa propre cle : il la recupere via
# GET /push/public-key (voir src/lib/pushApi.ts).
